const { expect } = require("chai");
const Traveler = require("../src/Traveler")
const Trips = require("../src/Trip")

  describe("Trips", () => {
    let trip
    beforeEach(() => {
      
      trip = new Trips()
    });
 
  })